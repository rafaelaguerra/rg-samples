<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rguerra.profiles">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.rguerra.profiles.ProfilesApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_action_add"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_neutral_profile"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.rguerra.profiles.view.activities.SplashScreenActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.rguerra.profiles.view.activities.MainActivity" />

        <activity android:name="com.rguerra.profiles.com.rguerra.presentation.view.activities.PostsActivity" />
    </application>

</manifest>
